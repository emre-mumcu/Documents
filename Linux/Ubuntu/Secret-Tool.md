SECRET TOOL
-----------
secret-tool is a command-line interface for the GNOME Keyring (or any libsecret-compatible keyring).

sudo apt update
sudo apt install libsecret-tools
secret-tool search service vscode
secret-tool clear service vscode


secret-tool lets you view, search, add, or delete these stored secrets from the terminal instead of using a GUI like Seahorse.
